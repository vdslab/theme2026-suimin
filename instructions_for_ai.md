# AIエージェント向け指示書：コーヒー豆の風味可視化および推薦Webサイトの開発

このドキュメントは、コーヒー豆の風味データを可視化し、ユーザーの好みに応じた豆の推薦を行うWebサイト開発を自動または半自動で行うための指示書（仕様書）です。

---

## 1. プロジェクト概要
ユーザーが「飲んだことのあるコーヒー豆（産地・精製方法）」をインプットとして与えると、その好みを分析し、膨大なコーヒー豆データの中から類似したおすすめの豆を2D散布図上で可視化して推薦するWebアプリケーションを構築します。

### 使用データ
- `data/merged_data_cleaned.csv`
  - コーヒー豆のデータセット。以下の味覚評価カラム（0〜10点）を含みます：
    - `Aroma`（香り）
    - `Flavor`（風味）
    - `Aftertaste`（後味）
    - `Acidity`（酸味）
    - `Body`（コク）
    - `Balance`（バランス）
  - その他、`Country.of.Origin`（産地）、`Processing.Method`（精製方法）、`Variety`（品種）などのメタデータが含まれます。

---

## 2. システム構成・アーキテクチャ

### 課題と対策（事前計算の導入）
UMAP（次元削減）およびHDBSCAN（ソフトクラスタリング）の計算は非常に重いため、ブラウザ側（JavaScript）でリアルタイムに実行すると初期読み込み時に大きな遅延が発生します。
**対策**: Pythonスクリプトを用いて事前にデータ処理・UMAP座標計算・HDBSCANクラスタリングを実行し、結果を静的JSONファイル（`src/data/coffee_data.json`）として出力します。フロントエンドは、このJSONを読み込んで描画するだけに留めることで、ロード時間を大幅に短縮（100ms未満）します。

```
[CSVデータ] (data/merged_data_cleaned.csv)
      │
      ▼ (Pythonによる事前計算: scripts/precompute_data.py)
[JSONデータ] (src/data/coffee_data.json)
      │
      ▼ (Reactフロントエンドでロード: App.jsx)
[2D散布図 & 推薦UI] (ブラウザ画面)
```

---

## 3. 各コンポーネントの要件

### A. データ処理・事前計算スクリプト (`scripts/precompute_data.py`)
1. **データの読み込みとフィルタリング**:
   - `data/merged_data_cleaned.csv` から `Country.of.Origin` と `Processing.Method` が欠損している行を除外します。
2. **ノードの集約**:
   - **産地（Country.of.Origin）と精製方法（Processing.Method）が同じ豆は一つのノードにまとめて描画する**ため、これらのカラムで `groupby` を行い、味覚スコアの平均値を算出します。
   - `Variety`（品種）は、同一グループ内に存在するユニークな品種のリスト（またはカンマ区切りの文字列）として集約します。
   - クラスタの安定性を確保するため、元のデータセットにおいて「同じ産地×精製方法」のサンプル数が3以上のグループに絞り込みます（Colabモックの基準を踏襲）。
3. **味覚スコアの正規化**:
   - 各グループの味覚スコアについて、全体の品質バイアスを除くために「行平均での除算（相対評価化）」を行った後、`StandardScaler` でスケーリングします。
4. **UMAPによる2次元投影**:
   - `n_neighbors=5, min_dist=0.05, metric='euclidean'` の設定で2次元空間に射影し、`UMAP_X`, `UMAP_Y` 座標を得ます。
5. **HDBSCANによるソフトクラスタリング**:
   - UMAP投影後の座標に対して `min_cluster_size=4, min_samples=3` 等でソフトクラスタリングを行います。
   - 各データ点の各クラスタへの所属確率（`membership_probability`）を取得します。
6. **クラスタ命名と色ブレンド**:
   - 各クラスタの平均特徴量から、以下のルールで自動命名します：
     - 香り（Aroma）が最大 ➔ `🌸 香り特化型`
     - コク（Body）が最大 ➔ `☕ ボディ・コク重視`
     - 風味・酸味（Flavor, Acidity）が最大 ➔ `🍋 風味・酸味際立ち`
     - その他 ➔ `⚖️ マイルド・調和型`
   - 各ノードのクラスタ所属確率に基づいてカラーブレンドを行います。所属確率が低い（1.0未満の残り）部分は `lightgrey`（ノイズ色）をブレンドします。
7. **JSONの保存**:
   - 以下の構造を持つ配列として `src/data/coffee_data.json` に保存します。
     ```json
     [
       {
         "id": "ethiopia_washed",
         "country": "Ethiopia",
         "method": "Washed / Wet",
         "varieties": ["Typica", "Bourbon"],
         "x": -1.23,
         "y": 4.56,
         "taste": {
           "Aroma": 8.5,
           "Flavor": 8.3,
           "Aftertaste": 8.2,
           "Acidity": 8.4,
           "Body": 8.0,
           "Balance": 8.1
         },
         "dominant_cluster": "🍋 風味・酸味際立ち",
         "color": "rgb(120, 200, 150)",
         "probs": {
           "cluster_0": 0.7,
           "cluster_1": 0.1,
           "noise": 0.2
         },
         "sample_count": 12
       }
     ]
     ```

### B. フロントエンド・可視化 (React/Vite/Tailwind CSS v4/DaisyUI)
1. **レイアウト**:
   - 画面左側：コントロールパネル（ユーザーが飲んだことのある豆の選択、推薦された豆のリスト表示、詳細情報カード）。
   - 画面右側：インタラクティブな2D散布図（UMAP座標）。
2. **インタラクティブ散布図**:
   - `src/data/coffee_data.json` から読み込んだデータを描画します。
   - CanvasまたはSVGを用いてカスタム描画し、以下の機能を提供します：
     - **ホバー**: 豆の詳細なメタデータ、各クラスタの所属確率、6次元の味覚スコアを美しいポップアップで表示。
     - **ズーム・パン**: マウスホイールやドラッグによるグラフの拡大縮小・移動。
     - **凡例フィルター**: 凡例をクリックすることで、対応するクラスタに属するノードのみを強調・非表示にできるトグル機能。
3. **「飲んだことのある豆」選択UI**:
   - ユーザーが「国名」と「精製方法」を入力・検索できるオートコンプリート検索ボックス（複数選択可能）。
4. **推薦アルゴリズム（フロントエンド）**:
   - ユーザーが選択した豆の味覚スコアの平均値を求め、ユーザーの「味覚プロファイル」を作成します。
   - この味覚プロファイルと、データベース内の他の全ノードとの距離（例：6次元味覚空間におけるコサイン類似度またはユースリッド距離）をJSで計算します。
   - 類似度が高い上位5〜10件のノードを「おすすめの豆」として抽出します。
5. **推薦結果の視覚的強調**:
   - 散布図上で、推薦された豆のノードを大きくし、周囲にアニメーションする光る輪（リング）を表示するなど、視覚的に強調します。
   - 選択された豆自体も異なるマーク（★マークなど）で強調します。
   - 左側パネルに推薦カードリストを表示し、クリックするとそのノードへ自動ズームする機能を提供します。
6. **デザイン・UI/UX品質**:
   - Tailwind CSS v4およびDaisyUIを駆使したモダンで美しいダーク/ライトモード対応インターフェース。
   - ガラスモルフィズム（背景ぼかし）や、ホバー時の滑らかなアニメーション転移など、プレミアムなデザインを徹底します。

---

## 4. 検証・品質保証プラン
- **データ一貫性の検証**:
  - `precompute_data.py` の生成する JSON が、元の CSV の集計値（Aroma などの平均値）と一致しているかをユニットテスト、または簡単なアサーションで確認する。
- **UIレスポンス**:
  - 初期ロード時の計算コストがなく、画面が瞬時に表示されることを確認する。
  - 豆の選択時、散布図の更新および推薦結果の再計算が1フレーム以内（16ms未満）でラグなく行われることを確認する。
